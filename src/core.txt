train
    df = load_data(data_path)
    df = process_data(df, label=True)
    train(data, model, sample=1.0)

predict
    for f in files:
        df = load_data(f)
        df = process_data(f)
    predict(model, input_path, output_path)